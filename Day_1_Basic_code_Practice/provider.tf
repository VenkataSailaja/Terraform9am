/*terraform {
  required_providers {
    aws ={
      source = "hashicorp/aws" #specific version of cloud provider to install from terraform registry
      version = "6.45.0"
    }
  }
} */

provider "aws" {
   #default cloud provider version from terraform registry
}